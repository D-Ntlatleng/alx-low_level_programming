#include <stdio.h>
#include <time.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * Description: 'the program's description'
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a;

	for (a = 97; a < 123; a++)
		putchar(a);
	for (a = 65; a < 91; a++)
		putchar(a);
	putchar('\n');
	return (0);
}
