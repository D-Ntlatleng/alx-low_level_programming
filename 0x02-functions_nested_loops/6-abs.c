#include "main.h"

/**
 * _abs - check lower case
 * @c: check if is lower
 *
 * Description: 'the program's description'
 *
 * Return: 1 (success) and 0 (failed)
 */

int _abs(int c)
{
	return (c >= 0 ? c : c * -1);
}
